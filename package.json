{
  "name": "autoflow",
  "version": "0.1.0",
  "private": true,
  "description": "Compañero de automatización para QAs del banco",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "record": "node scripts/start-recording.js",
    "run:test": "node scripts/run-test.js",
    "run:set": "node scripts/run-testset.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
